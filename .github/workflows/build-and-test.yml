name: build-and-test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      reports_repo:
        description: "REPORTS_REPO build-arg"
        default: "https://github.com/NeoIPC/Surveillance-Toolkit.git"
        required: false
      reports_branch:
        description: "REPORTS_BRANCH build-arg"
        default: "main"
        required: false
      neoipcr_repo:
        description: "NEOIPCR_REPO build-arg"
        default: "Brar/neoipcr"
        required: false
      neoipcr_branch:
        description: "NEOIPCR_BRANCH build-arg"
        default: "TowardsReferenceReport"
        required: false

# Cancel in-progress runs of this workflow on the same ref. A push that
# obsoletes a still-running PR build shouldn't waste minutes finishing.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changelog-section:
    # Directory.Build.props' <Version> must already be described in CHANGELOG.md. The release workflow
    # enforces the same thing, but only once a tag exists — and a tag here is immutable (the retag
    # guard rejects a moved one), so discovering a missing section at release time costs a version
    # number rather than an edit. Catching it on the pull request keeps it an edit.
    name: Changelog section for this version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - id: version
        name: Read the version from Directory.Build.props
        # Read as XML, not with a regex over the text, as in the release workflow: a regex cannot
        # tell a live element from one inside a comment, and concatenates the values of two elements
        # into a version that exists nowhere. The parser sees exactly the elements MSBuild sees.
        shell: pwsh
        run: |
          $nodes = ([xml](Get-Content -Raw Directory.Build.props)).SelectNodes('/Project/PropertyGroup/Version')
          if ($nodes.Count -ne 1) {
            Write-Output "::error file=Directory.Build.props::Expected exactly one <Version> element, found $($nodes.Count)."
            exit 1
          }
          $file = $nodes[0].InnerText.Trim()
          if (-not $file) {
            Write-Output "::error file=Directory.Build.props::The <Version> element is empty."
            exit 1
          }
          "version=$file" >> $env:GITHUB_OUTPUT
          Write-Output "Directory.Build.props declares $file."
      - name: CHANGELOG.md describes the built version
        # Literal prefix match, as in the release workflow: a version carries `.` and `-`, which a
        # regex built from it would interpret, and the closing `]` is what stops `0.2.0` matching a
        # `0.2.0-rc.1` heading. A section ends at the next version heading or at a Keep a Changelog
        # link-reference line (`[x.y.z]: url`), so the trailing reference definitions never leak into
        # the last version's body.
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          ver="$VERSION"
          section="$(awk -v want="## [$ver]" '
            index($0, want) == 1 { inside = 1; next }
            inside && index($0, "## [") == 1 { exit }
            inside && $0 ~ /^\[[^]]+\]: / { exit }
            inside { print }
          ' CHANGELOG.md)"
          if [ -z "$(printf '%s' "$section" | tr -d '[:space:]')" ]; then
            echo "::error file=CHANGELOG.md::CHANGELOG.md has no non-empty '## [$ver]' section, but Directory.Build.props declares $ver. Describe the version before releasing it."
            exit 1
          fi
          echo "CHANGELOG.md documents $ver."

  text-hygiene:
    # LF line endings and UTF-8 without a BOM on every committed text file.
    #
    # Worth an explicit check because the defect is invisible locally: with `* text=auto` declared, a tool
    # writing CRLF into the working tree still produces an LF blob and a clean `git status`, so the index
    # can essentially never report it. The BOM half matters here in particular — Visual Studio and Rider
    # both add one to new files by default, which is where this repository's BOM'd C# came from.
    name: Text-file hygiene
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check line endings and encoding
        shell: pwsh
        run: |
          $failures = @()
          # git ls-files --eol emits: i/<idx>  w/<worktree>  attr/<attrs><TAB><path>. Split the path off the
          # TAB first so an attribute containing a space cannot shift the column indices.
          $rows = foreach ($line in (git ls-files --eol)) {
            $parts = $line -split "`t", 2
            if ($parts.Count -lt 2) { continue }
            $cols = @($parts[0] -split '\s+' | Where-Object { $_ })
            if ($cols.Count -lt 2) { continue }
            $attr = if ($cols.Count -gt 2) { ($cols[2..($cols.Count - 1)] -join ' ') -replace '^attr/', '' } else { '' }
            # Only the ATTRIBUTE exempts a path. The i/ and w/ columns report what git DETECTED, and folding
            # them in here would let one stray byte skip the line-ending, BOM and UTF-8 checks at once — a
            # single NUL or even a lone CR makes git report -text — so a CRLF UTF-16 file would commit
            # silently, which is the corruption this job exists to catch.
            [pscustomobject]@{
              Index = $cols[0]; Worktree = $cols[1]; File = $parts[1]
              IsBinary = ($attr -match '(^|\s)(-text|binary)(\s|$)')
              LooksBinary = ($cols[0] -eq 'i/-text' -or $cols[1] -eq 'w/-text')
            }
          }
          foreach ($row in $rows) {
            if ($row.IsBinary) { continue }
            # Declared text but detected binary: report it, because nothing else can see this defect — git
            # prints -text rather than crlf, a NUL is valid UTF-8, and UTF-16 carries no UTF-8 BOM.
            if ($row.LooksBinary) {
              $failures += "$($row.File): declared text but git detects binary content (a NUL byte or stray CR) - probably mis-encoded; declare it '-text' if it really is binary"
              continue
            }
            if ($row.Worktree -in @('w/crlf', 'w/mixed')) {
              $failures += "$($row.File): $($row.Worktree.Substring(2)) line endings in the working tree"
            } elseif ($row.Index -in @('i/crlf', 'i/mixed')) {
              $failures += "$($row.File): $($row.Index.Substring(2)) line endings in the index"
            }
          }
          $strict = [System.Text.UTF8Encoding]::new($false, $true)
          foreach ($row in ($rows | Where-Object { -not $_.IsBinary })) {
            $full = Join-Path (Get-Location) $row.File
            if (-not (Test-Path -LiteralPath $full -PathType Leaf)) { continue }
            # Absolute path: .NET resolves a relative one against the process working directory, not
            # PowerShell's location, so a relative path here reads the wrong file or nothing at all.
            $bytes = [System.IO.File]::ReadAllBytes($full)
            if ($bytes.Length -ge 3 -and $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) {
              $failures += "$($row.File): starts with a UTF-8 BOM"; continue
            }
            try { [void]$strict.GetString($bytes) } catch { $failures += "$($row.File): not valid UTF-8" }
          }
          if ($failures.Count) {
            $failures | Sort-Object | ForEach-Object { Write-Output "::error file=$($_.Split(':')[0])::$_" }
            Write-Output "Expected LF line endings and UTF-8 without a BOM. See .gitattributes and .editorconfig."
            exit 1
          }
          # A run that inspected nothing must not report success. With no rows, every check above passes
          # vacuously, so this job would print OK and exit 0 having looked at nothing — a result
          # indistinguishable from a healthy checkout, on exactly the defect it exists to catch.
          $checked = @($rows | Where-Object { -not $_.IsBinary })
          if ($checked.Count -eq 0) {
            # Two states reach here and they need different fixes, so the message must not name only one:
            # the repository tracks nothing at all, or it tracks files and git reports every one of them
            # binary — which a .gitattributes regression produces, and which is the more likely and the
            # more misleading, since the checkout looks entirely normal.
            Write-Output '::error::Cannot check text-file hygiene: nothing was inspected. Either no paths are tracked, or git reports every tracked path as binary — check .gitattributes before assuming the checkout is empty.'
            exit 1
          }
          Write-Output "Text-file hygiene OK: $($checked.Count) file(s)."

  unit:
    # Reusable unit + generator test job, shared with release.yml so a released image runs the same tests.
    uses: ./.github/workflows/unit-tests.yml

  dockerfile:
    # Validate that the Dockerfile builds without running the full
    # integration suite. --target r-runtime exercises the shared parent
    # stage the r-runtime refactor introduced; --target build exercises
    # the .NET build stage and everything it transitively pulls in.
    name: Dockerfile sanity build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - name: Build r-runtime stage
        run: >
          docker build
          -f src/NeoIPC.Reporting/Dockerfile
          --target r-runtime
          -t neoipc-reporting:r-runtime
          .
      - name: Build .NET build stage
        run: >
          docker build
          -f src/NeoIPC.Reporting/Dockerfile
          --target build
          -t neoipc-reporting:build
          .

  container:
    # Container smoke tests (NegativePathTests / ParametersEndpointTests):
    # build the image and exercise it in isolation via Testcontainers with a
    # placeholder session. This is the repository's own self-contained gate —
    # it needs Docker and a built image, so it runs on manual dispatch (the
    # image build is expensive; the dockerfile job above already covers build
    # sanity on every push/PR). The shared-seeded-stack tests
    # (Category=Integration, RenderingIntegrationTests) are driven from the
    # workspace integration entrypoint, not from this repository's CI.
    name: Container smoke tests
    if: github.event_name == 'workflow_dispatch'
    needs: unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
          dotnet-quality: preview
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build image
        run: |
          docker build \
            -f src/NeoIPC.Reporting/Dockerfile \
            --build-arg REPORTS_SOURCE=github-branch \
            --build-arg REPORTS_REPO="${{ inputs.reports_repo }}" \
            --build-arg REPORTS_BRANCH="${{ inputs.reports_branch }}" \
            --build-arg NEOIPCR_SOURCE=github-branch \
            --build-arg NEOIPCR_REPO="${{ inputs.neoipcr_repo }}" \
            --build-arg NEOIPCR_BRANCH="${{ inputs.neoipcr_branch }}" \
            -t neoipc-reporting:integration-test \
            .
      - name: Restore
        run: dotnet restore tests/NeoIPC.Reporting.Tests/NeoIPC.Reporting.Tests.csproj
      - name: Test (Category = Container)
        env:
          NEOIPC_REPORTING_IMAGE_TAG: neoipc-reporting:integration-test
        run: >
          dotnet test tests/NeoIPC.Reporting.Tests/NeoIPC.Reporting.Tests.csproj
          --no-restore
          --filter "Category=Container"
          --logger "trx;LogFileName=container-results.trx"
          --results-directory TestResults
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: container-test-results
          path: TestResults/
