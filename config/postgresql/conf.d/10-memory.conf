shared_buffers = 256MB          # 25% of available RAM; increase for production
work_mem = 8MB                  # per sort/hash operation
maintenance_work_mem = 64MB
effective_cache_size = 768MB    # ~75% of available RAM; increase for production

# max_wal_size = 2GB
